# @package _global_
# Ablação v3: lr/weight_decay menores
defaults:
  - /face_embedder: mediapipe
  - override /model: multimodal_hetero_face
  - override /audio_embedder: wav2vec2
  - override /text_embedder: roberta_emotion
  - override /trainer: lightning

experiment_name: abaw-ah-multimodal-face-v3

data:
  num_workers: 6
  batch_size: 16

trainer:
  max_epochs: 2500
  patience: 100
  monitor: val_f1_macro
  mode: max
  accumulate_grad_batches: 4

model:
  lr: 5e-4
  weight_decay: 5e-3

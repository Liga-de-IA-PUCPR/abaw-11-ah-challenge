# configs/experiment/rf_baseline.yaml
# @package _global_
# Baseline barato: RandomForest + prosódia librosa + caminho sklearn (CPU).
defaults:
  - override /model: random_forest
  - override /audio_embedder: librosa
  - override /trainer: sklearn
